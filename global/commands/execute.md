# /execute

Use the executor agent to act on a plan doc.

1. Read `docs/plans/$ARGUMENTS.md`
2. Show the delegation preview — which agents/skills run and in what order
3. Wait for go-ahead
4. Execute and tick off steps in the plan doc

$ARGUMENTS
